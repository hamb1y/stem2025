---
// src/components/Card.astro
const { title, thumbnail, url } = Astro.props;
---

<a href={url} class="card">
  {thumbnail && (
    <img src={thumbnail} alt={title} class="card-thumb" />
  )}
  <div class="card-content">
    <h2 class="card-title">{title}</h2>
    <p class="card-text">
      <slot />
    </p>
  </div>
</a>



